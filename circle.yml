test:
  override:
    - phpunit SampleTest
machine:
   php:
     version: 5.6.23 
deployment:
  master:
    branch: master
    commands:
      - echo "master branch deploy start"
  develop:
    branch: develop
    commands:
      - echo "develop branch deploy start"